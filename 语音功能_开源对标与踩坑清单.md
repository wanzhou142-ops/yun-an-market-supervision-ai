# 语音功能：开源项目对标 + 踩坑清单 + 我们的改造路线

> 生成日期：2026-08-04
> 对标对象：GitHub 上 5 个同类实时语音项目
> 落地对象：`voice-service/`（自研 ASR/TTS 服务）+ `dify-frontend/`（大屏前端）

---

## 一、找了哪些项目对标

| 项目 | 技术栈 | 对我们最有参考价值的点 |
|---|---|---|
| **xinnan-tech/xiaozhi-esp32-server** | Python + WebSocket，SileroVAD / FunASR / EdgeTTS，**LLM 可直连 Dify** | 和我们同源（小智生态）。插件化选型 + 唤醒/手动/打断三种对话模式 + 官方给出的组件测速数据 |
| **Castrozan/voice-pipeline** | Silero VAD + Deepgram + OpenAI TTS，六边形架构 | **明确的 5 态状态机**（AMBIENT→LISTENING→THINKING→SPEAKING→CONVERSING），把老版「6 秒切片 → 10 秒延迟 → 无打断」重构到 1~1.5 秒。**它的老版本 = 我们现在的版本** |
| **FrancisDeng/chat-bot** | Python WebSocket，VAD/ASR/LLM/TTS 全模块可插拔 | 服务端 VAD + 1 秒静音触发；打断后把上一轮问题与本轮补充合并成完整上下文 |
| **eauchs/speech-to-speech-pipeline** | asyncio + 队列 + 音频分块，Faster-Whisper + Kokoro | 用 asyncio 队列做流水线，barge-in 靠取消队列实现 |
| **JavaGuide / ai-voice.md**（工程化长文） | 级联方案 vs 原生 Realtime 对比 | 最有价值的一篇。给了具体参数（静音 400ms、分块 200ms）和真实失败教训 |

---

## 二、他们踩过的坑（按「我们会不会踩」标注）

### A 类：架构级 —— 我们已经踩在里面了

| # | 坑 | 谁踩的 | 我们的状态 |
|---|---|---|---|
| A1 | **固定时长切片录音**（录满 N 秒才处理）→ 用户说不完被截断、说完了还得干等 | voice-pipeline 老版（6 秒切片，总延迟 ~10s） | 🔴 **正在踩**。`lib/voice.ts:165-167` 固定约 6 秒（可手动再点麦克风提前停，但没有自动断句） |
| A2 | **整段 TTS**：等 LLM 全部输出完，再整段合成，再播放 → 延迟三段叠加 | 多个项目早期版本 | 🔴 **正在踩**。`lib/voice.ts:260-288` fetch 完整音频再播 |
| A3 | **HTTP 单次请求代替流式**，无法边收边播 | —— | 🔴 **正在踩**。`voice-service/server.py` 纯 REST，无 WebSocket |
| A4 | 「每段多 200ms，整轮就慢半拍」——延迟是**累加**的，要盯端到端 P95 不是单组件 | JavaGuide | ⚠️ 我们现在端到端粗估 **8~12 秒**，展厅场景不可接受 |
| A5 | 打断失败往往不是播放器问题，是**状态机没设计**：播放停了但服务端还在推流；LLM 停了但历史把没播的当已说 | JavaGuide | 🟡 我们还没做打断，但一做就会踩 |

### B 类：浏览器级 —— 大屏前端一定会遇到

| # | 坑 | 后果 | 我们的状态 |
|---|---|---|---|
| B1 | `getUserMedia` / `SpeechRecognition` **必须安全上下文**（https 或 localhost）。局域网 IP `http://192.168.x.x:3000` 直接拒绝 | 麦克风彻底不可用 | 🟢 暂时安全（deploy 包本机 localhost 启动）。⚠️ **谁要是改成局域网 IP 访问就当场废掉** |
| B2 | `AudioContext` 在页面加载时创建会是 `suspended` 状态，必须在**用户手势**里 `resume()` | 「界面正常但一点声音没有」 | 🟡 我们大屏是自动全屏+自动播放视频，**高风险** |
| B3 | `recognition.start()` 必须由 click/touch 真实手势触发，页面 onload 自动调用**静默失败、连错都不抛** | 排查起来极其痛苦 | 🟢 我们有麦克风按钮，符合要求 |
| B4 | Web Speech API 依赖 Google 后端，**国内不可用**；Safari 不支持 | —— | 🟢 已规避，默认走 `server` provider |
| B5 | ASR 会话还没 ready 就开麦 → 「第一句能说，第二句录不到」 | 现场翻车 | 🟡 需要加健康检查门禁 |

### C 类：现场级 —— 展厅特有，最容易翻车

| # | 坑 | 说明 |
|---|---|---|
| C1 | **回声自激**：音响外放 TTS → 麦克风收到 → ASR 识别成用户提问 → 机器和自己无限对话。浏览器 `echoCancellation` 只在「同机播放+近场」有效，**外接功放、大音量、远距离时基本失效** |
| C2 | VAD 漏检短语音（<1 秒的「嗯」「对」「不是」），也容易被远场人声/空调噪声误触发 |
| C3 | 静音判定阈值两难：调低（200ms）→ 用户自然停顿被切断；调高（1000ms）→ 反应慢。小智官方 FAQ 明确写「说话慢老被抢话就把 `min_silence_duration_ms` 调到 1000」 |
| C4 | 不同设备/浏览器 AEC 差异大，必须**在真实场地用真实音响测**，办公室测过不算数 |

> 📌 C1 我们已经踩过一次并修好了：08-04 发现 `ScriptProcessorNode.connect(audioCtx.destination)` 把麦克风信号回环到扬声器造成自激，录音里全是脉冲噪声。**已删掉这行**，只连 source。
> 现在靠「AI 说话时不收音」的半双工规避回声，但**外接大功率音响 + 高音量的现场条件还没实测过**，进场必须复测一次。

### D 类：合规/依赖级 —— 交付客户前必须过一遍

| # | 坑 | 我们的暴露面 |
|---|---|---|
| D1 | **Piper TTS 原仓库 2025-10 已归档**，后继 `piper1-gpl` 是 **GPL-3.0**，商用需要法务评估 | 🔴 **我们默认 TTS 就是 Piper**（`zh_CN-huayan-medium.onnx`）。这是给政府单位交付的项目，建议提前确认或备好替代（EdgeTTS / 火山豆包 / 阿里云 TTS） |
| D2 | EdgeTTS 走代理时经常超时失败 | 🟡 我们仅调试用，交付走离线，问题不大 |
| D3 | Realtime API 模型名写死在代码里，厂商版本迭代快 | 🟢 不涉及 |

---

## 三、他们的框架思路：三件套

看下来，所有做得好的项目都是同一套骨架，只是实现语言不同。

### 1. 管道（Pipeline）—— 全链路流式，不等「整段」

```
麦克风 → [VAD 判断说没说完] → ASR(流式) → LLM(SSE 流式)
                                              ↓ 按句切分
                                          TTS(逐句合成) → 播放队列(边收边播)
```

关键不是用了什么模型，而是**每一环都不等上一环全部完成**。特别是 LLM→TTS 这段：LLM 每吐出一个完整句子（遇到 `。！？；\n`）就立刻送去 TTS，播放队列串起来。这一个改动通常能砍掉 50% 以上的感知延迟。

### 2. 状态机 —— 打断的本质是「取消语义」

voice-pipeline 的设计（推荐直接抄）：

```
AMBIENT   待机，只有 VAD 在跑
   ↓ 检测到人声 / 点击麦克风
LISTENING 收音中，靠 VAD 判断说完没
   ↓ 断句完成
THINKING  已提交给 LLM，等首个 token
   ↓ 首句就绪
SPEAKING  播放 TTS，此时 VAD 仍在监听（用于打断）
   ↓ 说完
CONVERSING 追问窗口（~15 秒，此期间不用再唤醒）
   ↓ 超时无人说话
AMBIENT
```

打断（barge-in）要**同时**取消三层，缺一层就会出怪事：
- **播放层**：停音频 + 清空播放队列（渐变降音量，别硬切）
- **生成层**：`AbortController` 掐掉 LLM SSE + 取消未合成的 TTS 任务
- **上下文层**：已播出去的算「AI 说过」，没播的**不能**写进对话历史

### 3. 控制信令 —— WebSocket 上跑 JSON 指令

音频走二进制帧，控制走 JSON：`{action: submit | cancel | pause}`，服务端回 `asr_ready` / `partial` / `final` / `tts_chunk`。这样前后端状态才对得齐。

---

## 四、落到我们项目：现状体检 + 改造清单

### 现状（Explore 实测）

| 环节 | 现在 | 位置 |
|---|---|---|
| 录音 | 固定 ~6 秒，无 VAD | `dify-frontend/lib/voice.ts:165-167` |
| 传输 | HTTP POST，非流式 | `app/api/voice/route.ts:16` → `localhost:8000/asr` |
| ASR | Vosk small 中文，本地离线 | `voice-service/server.py` |
| LLM | Dify，**已经是流式** ✅ | `lib/dify.ts` |
| TTS | Piper 本地，**整段合成** | `lib/voice.ts:260-288` |
| 打断 | 无 | —— |
| 唤醒词 | 无（靠点按钮） | `app/page.tsx:446-456` |

粗估端到端：**空等录音 6s + ASR 1s + Dify 全文 3~5s + TTS 整段 1~2s ≈ 11~14 秒**。展厅观众站着等这么久，基本会走掉。

### P0：三天内能做完，不动架构，收益最大

1. **句子级流式 TTS**（★ 最高性价比）
   `lib/dify.ts` 已经是流式了，在前端加一个缓冲：累积字符，遇到 `。！？；\n` 就立刻切一句丢给 `/api/voice/tts`，用一个播放队列顺序播。
   → 感知延迟从「等全文」变成「等第一句」，**砍掉 3~5 秒**。

2. **用 VAD 替换固定 6 秒**
   浏览器端用 `@ricky0123/vad-web`（Silero ONNX，约 2MB，纯前端）。`onSpeechEnd` 后**再等 400ms 静音确认**再提交（直接提交会把自然停顿当结束，这是 JavaGuide 明确点名的坑）。
   → 省掉最多 6 秒空等，用户说完立刻走。

3. **AudioContext 保险**：页面首次交互（点「开始体验」/切到交互模式）时 `audioContext.resume()`，防 B2 静音事故

4. **开麦门禁**：调 `/health` 确认 voice-service 起来了再允许点麦克风，否则按钮置灰

> ⚠️ **关于 `echoCancellation`：不要照抄网上教程改回 true。**
> 08-04 我们已经把 `echoCancellation / noiseSuppression / autoGainControl` 全部**显式关掉**了（`lib/voice.ts`），原因是 Vosk 小模型对浏览器降噪造成的失真很敏感，开着反而识别不出来。
> 这和多数教程说的「三个都开」冲突，但**在我们当前的半双工架构下是对的**——AI 播放时本来就不收音，不需要 AEC。
> 记住这是**两套配套方案，不能混搭**：
> - 方案甲（现在这套）＝ 关 AEC + 半双工 + 不做打断 → 识别率优先，✅ 推荐展厅用
> - 方案乙 ＝ 开 AEC + 全双工 + 做打断 → 必须在 AEC **之后**的音频流上跑 VAD，且 ASR 要换成抗失真更强的模型（FunASR/sherpa-onnx）
>
> 只要还没换 ASR 模型，就别单独把 AEC 打开——那会两头不讨好：识别更差，回声也未必压得住。

### P1：现场稳定性（进场前必做）

5. **保持半双工，暂时不做 barge-in**
   ⚠️ 这是我给的明确建议：**展厅大屏别急着上打断**。观众不太会跟大屏抢话，但音响外放导致的回声自激（C1）是会让机器自言自语的现场事故。做法就是最土的那个——AI 播放期间麦克风静默丢弃。
   我们现在就是半双工，**这条属于"别改坏"而不是"要新增"**。

6. **加「按住说话」(Push-to-Talk) 作为兜底**
   展厅噪声环境下 PTT 比 VAD 可靠得多。默认 VAD，界面上留一个「按住说话」按钮，现场噪声超预期时切过去。

7. **现场实测清单**：真音响 + 真音量 + 真距离，测回声自激、测嘈杂环境识别率。办公室测过不算。

8. **确认 Piper 许可证**（D1），或提前把 TTS 换成客户可接受的方案。`server.py` 已经是多引擎可切换，换起来不难。

### P2：想做得更好再说

9. voice-service 改 WebSocket + 流式 ASR（能出 partial 结果，屏幕上边说边显字，观感提升明显）
10. 上正经状态机 + 三层取消，做真 barge-in
11. Vosk small 识别率不够的话，换 sherpa-onnx 或 FunASR/SenseVoiceSmall（小智服务端默认就是它）

### 不建议做的

- ❌ 唤醒词：展厅噪声大，误触发比漏触发更尴尬。有触摸/按钮就够了
- ❌ 一上来就端云混合：JavaGuide 原话「不要第一天做端云混合」，先跑通一条链路
- ❌ 改用局域网 IP 访问大屏：会直接触发 B1，麦克风全废

---

## 五、一句话总结

我们现在的语音链路，正好等于 voice-pipeline 那个作者**重构掉的老版本**（固定切片 + 非流式 + 无打断 + ~10 秒延迟）。他给出的答案是「全链路流式 + 状态机」。

对我们来说，投入产出比最高的两刀：**句子级流式 TTS** 和 **VAD 替代固定 6 秒录音**。这两个做完，展厅体验就从「不能用」到「能用」。打断和 WebSocket 是锦上添花，回声自激和 Piper 许可证才是会让现场/交付翻车的真风险。
