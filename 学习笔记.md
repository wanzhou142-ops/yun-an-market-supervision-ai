# 学习笔记（个人速记）

> 与 `知识地图_学习路线.md` 同步维护；本文是随手笔记版，完整学习路线 / 自检题见知识地图。
> 更新：2026-07-29，随项目三场景前端落地后复盘。项目交付状态以 `项目进度与对接文档.md` 为准。

## ① 提示词工程
- 多轮迎宾：主动问姓 + 记"X先生/女士"；闲聊不再硬兜底"找不到条文"。
- "内心 OS / 动作提示"用中文全角括号包裹，前端朗读时自动跳过 → 数字人"表演感"。
- 小智考模式：5 题（3 选择 + 2 判断），语音判分容错（"第二个/必"都算 B）；真机实测 ABCD 可分辨，无需改天干。
- 对应：`Dify迎宾交互提示词.md`、`小智平台问学考提示词.md`

## ② RAG 知识库（关键坑）
- **经济模式 vs 高质量/混合检索**：经济模式每块只抽 10 关键词 → 换种说法就搜不到；高质量是向量语义检索。本项目踩过坑，已修。
- TopK 设 5（太少易漏条文）；4 部法放一个库检索没问题。
- 4 部法占位文本（约 25 万字）上线前必须换客户确认的现行有效版；Dify 与小智各传一份，改一处同步另一处。
- 对应：`小智平台问学考知识库说明.md`、`项目梳理_平台分工与进展.md`

## ③ 全栈前端 Next.js（已落地三场景）
- 一个页面靠 `?scene=welcome|corridor|pharmacy` 切三场景；数字人 SVG + 声波动画。
- TTS 朗读自动跳过括号 OS；麦克风 ASR 浏览器原生（开发机无外网不可用，待客户模型）。
- 改文案/动画 = "改前端第一笔 commit" 的好练手。
- 对应：`app/page.tsx`、`app/api/chat/route.ts`、`lib/dify.ts`

## ④ 前后端分离与安全
- API Key 只放服务端 `app/api/chat/route.ts` 读 `process.env`，前端只调自己的 `/api/chat` 代理。
- `.env.local` 用 VS Code 建（别用记事本，会偷偷加 .txt 后缀导致 git 不识别）；已被 `.gitignore` 排除。

## ⑤ 语音交互 ASR / TTS
- TTS（文本→语音）= 浏览器 `speechSynthesis`，已通；ASR（语音→文本）= `webkitSpeechRecognition`，依赖外网，当前不可用 → 生产接客户语音模型。
- 一句话：TTS 已能说，ASR 待听。

## ⑥ 平台选型与需求拆解
- 口诀：**屏上文字用 Dify，嘴上语音用小智**。
- 迎宾大厅/宣传廊/模拟药店全归 Dify；教学考（实体机器人）归小智（2026-07-28 经理确认）。
- 同一需求按"交互形态"拆分，比写某行代码更值钱。

## ⑦ Git 协作（正在学）
- `git add 具体文件`（别 `git add .`，避免误提交 `.env.local`）。
- `commit -m` 写清具体功能；`push` 不是一次性终态，可反复提交。
- 新功能开分支 + PR，复核后才进 main。

## ⑧ 项目交付与沟通
- 对接文档三件套：完成度对照需求表 + 对外话术 + 风险清单。
- 对经理讲分工/排期；对客户讲价值/配合项；法规与素材是占位，一键替换即可上线。

---

## 我能从这个项目学到什么（一句话版）
一个 **"Dify RAG 问答 + Next.js 前端 + 提示词工程 + 语音交互"** 的完整 AI 应用经验，外加 **"按交互形态做平台选型"** 的产品思维——正好是秋招 AI 应用方向最吃得开的履历组合。详细拆解见 `知识地图_学习路线.md` 与 `项目进度与对接文档.md` 第六节。
