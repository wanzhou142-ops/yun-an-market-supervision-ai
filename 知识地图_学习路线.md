# 知识地图 · 学习路线（从"了解"到"会做"）

> 抽取自 `项目进度与对接文档.md` 第六节，并重排为可照着练的学习流。
> 适用：秋招 AI 应用方向，零基础起步（TypeScript / Git 新手友好）。
> 用法：每个模块按 **学 → 练 → 验** 三步走；顶部打卡表标记进度。

---

## 📋 总览打卡表

| #   | 模块           | 对应项目真文件                                                            | 状态                      | 预计投入  |
| --- | ------------ | ------------------------------------------------------------------ | ----------------------- | ----- |
| ①   | 提示词工程        | `Dify迎宾交互提示词.md`、`小智平台问学考提示词.md`                                   | ✅ 已掌握                   | 半天    |
| ②   | RAG 知识库      | `小智平台问学考知识库说明.md`、`项目梳理_平台分工与进展.md`                                | ✅ 已掌握                   | 半天    |
| ③   | 全栈前端 Next.js | `dify-frontend/app/page.tsx`、`app/api/chat/route.ts`、`lib/dify.ts` | 🔵 练中（三场景已落地）           | 2~3 天 |
| ④   | 前后端分离与安全     | `dify-frontend/.gitignore`、`app/api/chat/route.ts`                 | ✅ 已掌握                   | 2 小时  |
| ⑤   | 语音交互 ASR/TTS | 前端 `lib/` 里的 speech 调用                                             | 🔵 练中（TTS 已通，ASR 待客户模型） | 2 小时  |
| ⑥   | 平台选型与需求拆解    | `云安区市场监管AI体验方案_内容整理.md` 第五节                                        | ✅ 已掌握                   | 半天    |
| ⑦   | Git 协作       | `dify-frontend/` 仓库                                                | 🔵 练中（正在学）              | 1 天   |
| ⑧   | 项目交付与沟通      | 本文件本身                                                              | ✅ 已掌握                   | 2 小时  |

> 状态填法：⬜ 待开始 → 🔵 练中 → ✅ 已掌握（能合上文档答出自检题）。
> 更新（2026-07-29）：本项目三场景前端、Dify RAG、提示词工程、一键部署包均已落地，状态表已同步为实际进度；"了解层"描述保留作复盘对照（见各模块标题）。

---

## ① 提示词工程（Prompt Engineering）—— 最贴近"AI 应用"核心

**你现在停在哪（复盘对照）**：知道"提示词"大概就是给 AI 写指令，但没亲手调过、没看过改一个词模型行为怎么变。

**学完你能做到**：给一个具体场景（迎宾 / 出题 / 判分）写出结构清晰的提示词，并在 Dify / 小智里实测生效。

### 动手步骤（跟着项目练）
1. 打开 `Dify迎宾交互提示词.md`，通读"多轮迎宾"那段，标出 3 个关键机制：角色设定、问姓记称呼、`(内心 OS / 动作提示)` 括号约定。
2. 打开 `小智平台问学考提示词.md`，重点看"考模式"——5 题怎么出、判分容错（"第二个/必"算 B）怎么写。
3. **改一个小地方练手**：把迎宾开场白里"X 先生/女士"的称呼规则，改成"加一句欢迎语"。在 Dify 的 LLM 节点替换提示词，问一句看回答是否变。
4. **观察差异**：故意把括号 OS 机制删掉，看数字人朗读时会不会把"（微笑）"念出来——理解"表演感"为什么需要这套约定。

### 自检题（答得出 = 真会了）
- 为什么"内心 OS"要用括号包起来、前端朗读时要跳过？不包会怎样？
- 考模式里"说'第二个'也算 B"解决了什么真实问题？（语音识别不像键盘精准）

### 求职落点
简历可写："为市场监管数字人设计多轮迎宾与问学考提示词，含语音判分容错规则。"

---

## ② RAG 知识库搭建（Dify 低代码）

**你现在停在哪（复盘对照）**：知道 RAG = "检索增强生成"、知识库能上传文档，但没自己建过、没踩过检索模式的坑。

**学完你能做到**：独立在 Dify 建一个知识库（上传→分段→索引→TopK），解释"为什么之前关键词检索换说法搜不到"，并跑通 Dify 聊天助手（advanced-chat）。

### 动手步骤
1. 打开 `小智平台问学考知识库说明.md`，看"检索要点"那段，理解 TopK 是什么。
2. 打开 `项目梳理_平台分工与进展.md`，找到"经济模式 vs 高质量/混合检索"的坑——**这是必考点**：经济模式是关键词匹配，换种说法就搜不到；高质量是向量语义检索。
3. **亲手建一遍**（用 `知识库占位资料/` 里任意 1 个 txt）：Dify 建知识库 → 自动分段 → 索引选"高质量+混合" → 等索引完成 → 建聊天助手绑这个库 → 问一句验证引用。
4. 对照实验：同一个问题，在"经济模式"和"高质量"下分别搜，记录差异，写进自己的笔记。

### 自检题
- "高质量索引"和"经济模式"本质区别是什么？为什么本项目踩了经济模式的坑？
- TopK=3 和 TopK=10 对回答有什么不同影响？

### 求职落点
简历可写："基于 Dify 搭建市场监管法规 RAG 问答，配置高质量混合检索，规避关键词检索召回失败。"

---

## ③ 全栈前端（Next.js + TypeScript + React）—— 你已落地三场景前端（继续练）

**你现在停在哪（复盘对照）**：能看懂页面长什么样，但不知道 `useState`/`useEffect`/`useRef` 各自干嘛、API 路由怎么把 Key 藏起来。

**学完你能做到**：说清本项目前端三场景怎么切换、TTS 怎么触发、Key 为什么不在前端暴露；并改一个小功能（如换开场白文案）。

### 动手步骤
1. 打开 `dify-frontend/app/page.tsx`，找 `?scene=welcome|corridor|pharmacy` 的读取逻辑——理解"一个页面靠参数切三场景"。
2. 打开 `app/api/chat/route.ts`，只看一件事：**这里怎么读 `process.env` 的 Key 并调 Dify**，前端永远不碰 Key。
3. 打开 `lib/dify.ts`，看它怎么发请求、怎么处理返回。
4. **改一处练手**：把迎宾场景的开场白文字改掉，本地 `npm run dev` 看页面是否变（这就是你"改前端"的第一笔 commit）。
5. 在浏览器控制台打印 `useRef` 绑定的数字人 DOM，理解"为什么朗读时要拿到这个元素做声波动画"。

### 自检题
- 为什么 API Key 必须放 `route.ts`（服务端）而不是 `page.tsx`（前端）？
- `useEffect` 和 `useState` 在本项目分别管什么？（一个管"副作用/朗读"，一个管"界面状态"）

### 求职落点
简历项目："Next.js + TypeScript 实现法治教育大屏前端，含场景切换、SVG 数字人、服务端代理保护 API Key。"

---

## ④ 前后端分离与安全常识

**你现在停在哪（复盘对照）**：听过"敏感信息别放前端"，但没在真实代码里见过正确写法，也没亲手建过 `.env.local`。

**学完你能做到**：指出本项目 Key 的正确藏法；用 VS Code（不是记事本！）正确新建 `.env.local` 并被 git 忽略。

### 动手步骤
1. 打开 `dify-frontend/.gitignore`，确认 `.env.local` 在里面——理解"被忽略 = 不会进仓库 = 不泄露"。
2. 回看 `app/api/chat/route.ts`，确认 Key 只在此处 `process.env.DIFY_API_KEY` 读取。
3. **亲手建配置**：用 VS Code 新建 `.env.local`（⚠️ 别用 Windows 记事本，它会偷偷加 `.txt` 后缀变成 `.env.local.txt`，git 不识别）。写入 `DIFY_API_KEY=xxx`。
4. `git status` 验证这个文件**不出现在待提交列表**（说明被忽略了）。

### 自检题
- 如果有人把 Key 写进 `page.tsx` 并部署，会发生什么风险？
- 为什么用记事本建 `.env.local` 会出事？正确做法是什么？

### 求职落点
面试能讲："前端只调自己的服务端代理，密钥走环境变量并 gitignore，避免硬编码泄露。"（这是加分项，很多应届生答不上来）

---

## ⑤ 语音交互原理（ASR / TTS）

**你现在停在哪（复盘对照）**：知道"语音能听能说"，但分不清 ASR 和 TTS 谁是谁、不知道浏览器原生实现的坑。

**学完你能做到**：用一句话区分 ASR/TTS；说清本项目 TTS 已可用、ASR 为什么当前不可用、生产为什么要接专业模型。

### 动手步骤
1. 在前端代码里找 `speechSynthesis`（TTS，文本→语音）和 `webkitSpeechRecognition`（ASR，语音→文本）两处调用，各读一遍。
2. 本地 `npm run dev`，触发一次 TTS 朗读，亲耳听到数字人"说话"——确认 TTS 可用。
3. 试一次麦克风 ASR，观察为什么失败（开发机无外网 / 无权限）——对照文档理解"为什么生产要接客户语音模型"。

### 自检题
- ASR 和 TTS 方向各是哪到哪？本项目哪个已通、哪个卡住？
- 浏览器原生语音识别的两个硬限制是什么？

### 求职落点
面试能讲："理解 ASR/TTS 链路，知晓浏览器原生方案的局限，具备接入专业语音模型的认知。"

---

## ⑥ 平台选型与需求拆解思维（比写代码更值钱）

**你现在停在哪（复盘对照）**：知道"文字用 Dify、语音用小智"这句口诀，但没独立拆过一个需求、没比过选型。

**学完你能做到**：拿到一个新需求，按"交互形态"拆给不同平台，并说清 Dify 快开 vs 深度定制的取舍。

### 动手步骤
1. 重读 `云安区市场监管AI体验方案_内容整理.md` 第五节（选型对比表）。
2. 回看 `项目梳理_平台分工与进展.md`，把 R1~R6 的"现归属平台"列抄一遍，体会"同一需求按交互形态拆分"的思路。
3. **做个小练习**：假设新增需求"参观者用手机扫码后收到法规科普短信"——你会分给哪个平台/技术？写下你的拆分理由（短信网关？后端脚本？）。

### 自检题
- 本项目为什么把"迎宾大厅/宣传廊/模拟药店"全归 Dify、"教学考"归小智？判断依据是什么（交互形态）？
- Dify 快开和服务器深度定制，各自的"投入/风险/边界"怎么比？

### 求职落点
这是"产品/架构思维"，面试讲需求拆解比讲某行代码更显档次。

---

## ⑦ Git 协作（你正在学的）

**你现在停在哪（复盘对照）**：知道 `git init/add/commit` 名词，但没在真实项目里反复提交过，对"分支/PR/main"还没体感。

**学完你能做到**：在 `dify-frontend/` 里正常 `add/commit/push`，理解 main 是基线、分支用于"同学复核后再合并"。

### 动手步骤
1. 进入 `dify-frontend/`，`git status` 看当前改了什么（你改的开场白应该在这）。
2. `git add 具体文件`（别用 `git add .` 一把抓，避免把 `.env.local` 误提交——即使被 ignore 也养成精确添加习惯）。
3. `git commit -m "改迎宾场景开场白文案"`（message 写清具体功能，方便同伴 review）。
4. `git push` —— 记住：**push 不是一次性终态**，以后还能继续 commit+push，历史是不断追加的时间线。
5. 后续新功能：开分支 `git checkout -b feat-xxx` → 改 → commit → push → 让同学看 PR，合并前不进 main。

### 自检题
- `main` 是什么？为什么新功能要用分支 + PR 而不是直接推 main？
- 如果 push 错了一个文件，怎么补救（下次 commit 覆盖 / 或 `git revert`）？

### 求职落点
协作能力是硬门槛。能讲清"分支 + PR 复核流程"的应届生很少，这是差异化优势。

---

## ⑧ 项目交付与沟通

**你现在停在哪（复盘对照）**：看过本文件的"对接话术"和"风险清单"，但没自己从零整理过一份。

**学完你能做到**：把"完成度对照需求 + 对外话术 + 风险清单"整理成一份对齐文档（就是本文件这类东西）。

### 动手步骤
1. 重读本文档第四节"对经理 / 对客户话术"和第五节"上线前必做清单"。
2. **自己写一版**：假设你是项目经理，给经理写 3 条"当前进度 + 需要您确认的事"，不抄原文，用自己的话。
3. 对照原文，看自己漏了哪类信息（通常漏"阻塞点/责任方"）。

### 自检题
- 对接文档为什么要有"完成度表 + 话术 + 风险清单"三件套？少一件会怎样？
- 对客户说话和对其经理说话，口径重点有什么不同？

### 求职落点
"能把技术进展翻译成老板/客户听得懂的话"——这是 lead 潜质，实习/校招都很吃香。

---

## 🎯 一句话总结你能带走的

一个 **"Dify RAG 问答 + Next.js 前端 + 提示词工程 + 语音交互"** 的完整 AI 应用项目经验，外加 **"按交互形态做平台选型"** 的产品思维——正好是当前秋招 AI 应用方向最吃得开的履历组合。

**学习心法**：这份路线不是让你"再看一遍"，而是每个模块都去**改一点、跑一下、答出自检题**。卡住就叫我，我陪你逐个打通。
