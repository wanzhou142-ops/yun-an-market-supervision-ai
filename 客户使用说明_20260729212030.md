# 云安区市场监管 AI 体验（大屏前端）— 客户试用包使用说明

## 一、怎么启动（客户零安装）
1. 把整个 `dify-frontend-deploy` 文件夹解压到任意位置（如桌面）。
2. 双击里面的 **`start.bat`**（Windows）。
3. 等待约 5 秒，会自动打开浏览器到「迎宾大厅」页面。

> 本包已自带 Node 运行环境和后端地址，**客户电脑无需安装任何软件**。
> Linux / Mac 用户请双击 `start.sh`。

## 二、三个场景怎么切换
直接改浏览器地址栏里 `scene=` 后面的词即可：
- 迎宾大厅：`http://localhost:3000?scene=welcome`
- 普法宣传廊：`http://localhost:3000?scene=corridor`
- 模拟药店：`http://localhost:3000?scene=pharmacy`

## 三、对话功能（重要前提）
- 系统的"问答"依赖后端 Dify 知识库，后端地址为内网 `zkt.medlibbot.com:8380`。
- **请在公司内网环境下使用**，问答才能正常返回市场监管法规内容。
- 若客户在外网 / 异地环境打开：页面和背景视频能看，但问答会连不上后端（这是网络限制，不是程序问题）。

## 四、停止
- 关闭浏览器，并在 `start.bat` 的黑窗口里按 `Ctrl + C`，或直接关掉该窗口。

## 五、建议用来验证需求的提问
- 药品与非药品怎么区分？
- 模拟药店有哪些违法违规情形？
- （宣传廊）想了解某类市场监管法规

## 六、当前是"占位演示版"
- 法规知识库为公开法规占位，正式上线前会替换为客户确认的现行有效版本。
- 背景视频为 AI 生成的简易占位，正式版会替换为实拍 / 脚本视频。
