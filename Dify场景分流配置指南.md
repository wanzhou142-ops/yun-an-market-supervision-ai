# Dify 场景分流配置指南（让三个场景答得不一样）

> 目标：让前端切到「迎宾大厅 / 宣传廊 / 模拟药店」时，Dify 后端回答的**角色与侧重明显不同**，客户能体验"三个智能体"。
> 适用：当前 Dify 应用 = `云安区市场监管普法助手`，类型 `advanced-chat`（聊天助手），已挂知识库（RAG 命中法条）。
> 前端状态：已改好，`/api/chat` 每条消息都带 `inputs: { scene }`（welcome / corridor / pharmacy），只等 Dify 侧定义 `scene` 变量即生效。

---

## 一、结论先讲：不用升级 Chatflow，聊天助手就够

聊天助手支持「会话变量」，提示词里用 `{{scene}}` 引用，前端通过 `inputs` 传入。
只要在 Dify 里**加一个会话变量 `scene` + 把提示词改成按场景分支**，三个场景就会答得不同。
（强分流=挂三个不同知识库才需 Chatflow，属二期增强，本期不做。）

---

## 二、Dify 后台操作步骤（照着点）

1. 浏览器登录你的 Dify → 工作室 → 打开应用 **「云安区市场监管普法助手」**。
2. 左侧「编排」→ 找到「提示词」区域（系统推理模式保持默认）。
3. **添加会话变量**：
   - 点「变量」/「+ 添加」→ 变量名 `scene`，类型 `String`（文本）。
   - 设置默认值 `welcome`（防止前端偶发未传时兜底）。
   - 注意：不要把 `scene` 设为"用户表单必填"，否则前端未传会报错；保持可选即可。
4. **替换提示词**为下方「三、现成提示词模板」全文（直接复制粘贴，含 `{{scene}}` 引用）。
5. 右上角「**保存**」→「**发布**」。（发布后新对话才生效，旧对话不变）
6. 验证：回到前端，切场景问同一个问题（如"药品和非药品怎么区分"），三个场景回答应明显不同。

> ⚠️ 发布是覆盖式：保存后务必点"发布"，否则只是草稿，线上对话不会变。

---

## 三、现成提示词模板（直接复制）

```
你是「云安区市场监管综合培训法治教育基地」的 AI 普法助手，服务于迎宾大厅、普法宣传廊、模拟药店三个场景。

当前场景由变量 {{scene}} 决定，取值：welcome（迎宾大厅）、corridor（宣传廊）、pharmacy（模拟药店）。

【通用要求】
- 回答必须依据市场监管相关法律法规，引用法条时说明法规名称与具体条款。
- 语气专业、简明，面向普法培训与考核对象。
- 不确定时如实说明，不编造法规内容。

【按场景切换角色与侧重】
- 当 {{scene}}=welcome（迎宾大厅）：你是"引导者"。先礼貌问候、自报身份（云安市场监管普法助手），引导访客选择方向（迎宾介绍 / 法规科普 / 模拟药店实操），并提示可切换场景。不做长篇法条，多做流程引导。
- 当 {{scene}}=corridor（宣传廊）：你是"科普讲解员"。侧重法规条文科普、典型案例、经营者与消费者的权利与义务，引用具体法条并解释其意义。
- 当 {{scene}}=pharmacy（模拟药店）：你是"实操指导员"。侧重药品与非药品区分、处方药/非处方药管理、药品标识、常见违法违规情形（无证经营、超范围经营、虚假宣传、处方药违规销售），结合模拟药店情境给出判断与建议。

当前场景：{{scene}}。请严格按上述对应角色与侧重作答，不要串场景。
```

---

## 四、前端侧（已就绪，无需再改）

- `lib/dify.ts`：`sendToDify` 已把 `scene` 填入请求 `inputs: { scene }`。
- `app/page.tsx`：顶部 3 个场景 tab；切换时清空对话 + 重播开场白 + 把 `scene` 同步进 URL。
- 发消息时 `body` 带 `scene` → `/api/chat` → Dify。

> 也就是说：Dify 里 `scene` 变量一定义、提示词一发布，前端传的值立刻生效，无需重新打包。
> 但如果你改了前端代码后才要发客户，记得重新 `npm run build` + 重新打 deploy zip（见《项目进度与对接文档》R6 一节）。

---

## 五、验证清单（发客户前自测）

- [ ] Dify 变量 `scene` 已建（默认 welcome）
- [ ] 提示词已替换为模板并**发布**
- [ ] 前端切到 welcome，问"你们基地是干什么的" → 偏引导介绍
- [ ] 前端切到 corridor，问"经营者有哪些义务" → 偏法条科普
- [ ] 前端切到 pharmacy，问"药品和非药品怎么区分" → 偏实操判断
- [ ] 三个场景回答明显不同 = 分流生效 ✅

---

## 六、二期增强（可选，非本期必须）

若客户要求"三个场景用**完全不同的知识库**"（迎宾知识 / 普法知识 / 药品知识各自独立），则需：
1. 把应用从「聊天助手」升级为「Chatflow 工作流」；
2. 加「条件分支」节点按 `scene` 路由到三套不同知识库检索；
3. 工作量大、需重新设计编排，建议作为二期，不阻塞本期交付。
